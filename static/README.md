# static

Frontend static assets for CyberLab.

## Files

- `style.css`: Main global stylesheet used across all pages

## Notes

The UI currently uses server-rendered templates with shared CSS (no frontend build pipeline required).
